[package]
name = "contracts"
version = "0.1.0"
edition = "2024"
rust-version = "1.97"
publish = false

[lints]
workspace = true

[dependencies]
contracts-derive = { path = "../contracts-derive" }

[dev-dependencies]
trybuild = "1"
