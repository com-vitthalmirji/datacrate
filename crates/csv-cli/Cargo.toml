[package]
name = "csv-select-cli"
version = "0.1.2"
edition = "2024"
rust-version = "1.97"
description = "Streaming CSV column-selection CLI (csv-select)"
license = "MIT"
repository = "https://github.com/com-vitthalmirji/datacrate"

[lints]
workspace = true

[dependencies]
clap = { version = "4", features = ["derive"] }
csv = "1"
